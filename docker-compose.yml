version: "3.9"

services:
  backend:
    build: .
    container_name: eventora-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis
    volumes:
      - .:/app
      - /app/node_modules

  redis:
    image: redis:7
    ports:
      - "6379:6379"